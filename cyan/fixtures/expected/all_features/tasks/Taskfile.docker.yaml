version: '3'
tasks:
  build:
    desc: 'Build & push the Docker image (cached, tagged per commit)'
    cmds:
      - ./scripts/ci/docker.sh
  lint:
    desc: 'Lint the Dockerfile'
    cmds:
      - hadolint infra/Dockerfile
